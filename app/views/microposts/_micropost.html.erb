<li>
  <span class="ridedate"><%= micropost.ridedate %></span>出発</br>
  <span class="ride_on_flag"><%= micropost.ride_on_flag %></span>
  <span class="from"><%= micropost.from %></span>から  <span class="to"><%= micropost.to %></span>まで
  
  <span class="can_ride_person"><%= micropost.can_ride_person %></span>人乗せられます
  <span class="content"><%= micropost.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
  </span>

  <% if current_user?(micropost.user) %>
    <%= link_to "delete", micropost, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: micropost.content %>
  <% end %>
</li>