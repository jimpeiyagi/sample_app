<li id="<%= feed_item.id %>">
  <%= link_to gravatar_for(feed_item.user), feed_item.user %>
    <span class="user">
      <%= link_to feed_item.user.name, feed_item.user %>
    </span>
      <span class="ridedate"><%= feed_item.ridedate %></span>出発</br>
  <span class="ride_on_flg"><%= feed_item.ride_on_flag %></span>
  <span class="from"><%= feed_item.from %></span>から  <span class="to"><%= feed_item.to %></span>まで
  
  <span class="can_ride_person"><%= feed_item.can_ride_person %></span>人乗せられます
    <span class="content"><%= feed_item.content %></span>
    <span class="timestamp">
      Posted <%= time_ago_in_words(feed_item.created_at) %> ago.
    </span>
  <% if current_user?(feed_item.user) %>
    <%= link_to "delete", feed_item, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: feed_item.content %>
  <% end %>
</li>